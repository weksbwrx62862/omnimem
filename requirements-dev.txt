# OmniMem 开发依赖
# 安装: pip install -r requirements-dev.txt

# 包含核心依赖
-r requirements.txt

# 测试框架
pytest>=7.0.0
pytest-asyncio>=0.21.0
pytest-cov>=4.0.0

# 代码质量
ruff>=0.1.0
mypy>=1.5.0
types-PyYAML>=6.0
pre-commit>=3.0.0

# 可选依赖（开发时建议安装）
cryptography>=42.0.0
sentence-transformers>=2.2.0

# ★ M9-20: REST(FastAPI) 测试依赖（对应 omnimem[api] extra）
fastapi>=0.110.0
uvicorn>=0.27.0
